// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"faceto-ai/internal/data/ent/predicate"
	"faceto-ai/internal/data/ent/roomvod"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// RoomVodQuery is the builder for querying RoomVod entities.
type RoomVodQuery struct {
	config
	ctx        *QueryContext
	order      []roomvod.OrderOption
	inters     []Interceptor
	predicates []predicate.RoomVod
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the RoomVodQuery builder.
func (rvq *RoomVodQuery) Where(ps ...predicate.RoomVod) *RoomVodQuery {
	rvq.predicates = append(rvq.predicates, ps...)
	return rvq
}

// Limit the number of records to be returned by this query.
func (rvq *RoomVodQuery) Limit(limit int) *RoomVodQuery {
	rvq.ctx.Limit = &limit
	return rvq
}

// Offset to start from.
func (rvq *RoomVodQuery) Offset(offset int) *RoomVodQuery {
	rvq.ctx.Offset = &offset
	return rvq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (rvq *RoomVodQuery) Unique(unique bool) *RoomVodQuery {
	rvq.ctx.Unique = &unique
	return rvq
}

// Order specifies how the records should be ordered.
func (rvq *RoomVodQuery) Order(o ...roomvod.OrderOption) *RoomVodQuery {
	rvq.order = append(rvq.order, o...)
	return rvq
}

// First returns the first RoomVod entity from the query.
// Returns a *NotFoundError when no RoomVod was found.
func (rvq *RoomVodQuery) First(ctx context.Context) (*RoomVod, error) {
	nodes, err := rvq.Limit(1).All(setContextOp(ctx, rvq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{roomvod.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (rvq *RoomVodQuery) FirstX(ctx context.Context) *RoomVod {
	node, err := rvq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first RoomVod ID from the query.
// Returns a *NotFoundError when no RoomVod ID was found.
func (rvq *RoomVodQuery) FirstID(ctx context.Context) (id uint64, err error) {
	var ids []uint64
	if ids, err = rvq.Limit(1).IDs(setContextOp(ctx, rvq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{roomvod.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (rvq *RoomVodQuery) FirstIDX(ctx context.Context) uint64 {
	id, err := rvq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single RoomVod entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one RoomVod entity is found.
// Returns a *NotFoundError when no RoomVod entities are found.
func (rvq *RoomVodQuery) Only(ctx context.Context) (*RoomVod, error) {
	nodes, err := rvq.Limit(2).All(setContextOp(ctx, rvq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{roomvod.Label}
	default:
		return nil, &NotSingularError{roomvod.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (rvq *RoomVodQuery) OnlyX(ctx context.Context) *RoomVod {
	node, err := rvq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only RoomVod ID in the query.
// Returns a *NotSingularError when more than one RoomVod ID is found.
// Returns a *NotFoundError when no entities are found.
func (rvq *RoomVodQuery) OnlyID(ctx context.Context) (id uint64, err error) {
	var ids []uint64
	if ids, err = rvq.Limit(2).IDs(setContextOp(ctx, rvq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{roomvod.Label}
	default:
		err = &NotSingularError{roomvod.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (rvq *RoomVodQuery) OnlyIDX(ctx context.Context) uint64 {
	id, err := rvq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of RoomVods.
func (rvq *RoomVodQuery) All(ctx context.Context) ([]*RoomVod, error) {
	ctx = setContextOp(ctx, rvq.ctx, "All")
	if err := rvq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*RoomVod, *RoomVodQuery]()
	return withInterceptors[[]*RoomVod](ctx, rvq, qr, rvq.inters)
}

// AllX is like All, but panics if an error occurs.
func (rvq *RoomVodQuery) AllX(ctx context.Context) []*RoomVod {
	nodes, err := rvq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of RoomVod IDs.
func (rvq *RoomVodQuery) IDs(ctx context.Context) (ids []uint64, err error) {
	if rvq.ctx.Unique == nil && rvq.path != nil {
		rvq.Unique(true)
	}
	ctx = setContextOp(ctx, rvq.ctx, "IDs")
	if err = rvq.Select(roomvod.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (rvq *RoomVodQuery) IDsX(ctx context.Context) []uint64 {
	ids, err := rvq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (rvq *RoomVodQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, rvq.ctx, "Count")
	if err := rvq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, rvq, querierCount[*RoomVodQuery](), rvq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (rvq *RoomVodQuery) CountX(ctx context.Context) int {
	count, err := rvq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (rvq *RoomVodQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, rvq.ctx, "Exist")
	switch _, err := rvq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (rvq *RoomVodQuery) ExistX(ctx context.Context) bool {
	exist, err := rvq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the RoomVodQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (rvq *RoomVodQuery) Clone() *RoomVodQuery {
	if rvq == nil {
		return nil
	}
	return &RoomVodQuery{
		config:     rvq.config,
		ctx:        rvq.ctx.Clone(),
		order:      append([]roomvod.OrderOption{}, rvq.order...),
		inters:     append([]Interceptor{}, rvq.inters...),
		predicates: append([]predicate.RoomVod{}, rvq.predicates...),
		// clone intermediate query.
		sql:  rvq.sql.Clone(),
		path: rvq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		UUID string `json:"uuid,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.RoomVod.Query().
//		GroupBy(roomvod.FieldUUID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (rvq *RoomVodQuery) GroupBy(field string, fields ...string) *RoomVodGroupBy {
	rvq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &RoomVodGroupBy{build: rvq}
	grbuild.flds = &rvq.ctx.Fields
	grbuild.label = roomvod.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		UUID string `json:"uuid,omitempty"`
//	}
//
//	client.RoomVod.Query().
//		Select(roomvod.FieldUUID).
//		Scan(ctx, &v)
func (rvq *RoomVodQuery) Select(fields ...string) *RoomVodSelect {
	rvq.ctx.Fields = append(rvq.ctx.Fields, fields...)
	sbuild := &RoomVodSelect{RoomVodQuery: rvq}
	sbuild.label = roomvod.Label
	sbuild.flds, sbuild.scan = &rvq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a RoomVodSelect configured with the given aggregations.
func (rvq *RoomVodQuery) Aggregate(fns ...AggregateFunc) *RoomVodSelect {
	return rvq.Select().Aggregate(fns...)
}

func (rvq *RoomVodQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range rvq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, rvq); err != nil {
				return err
			}
		}
	}
	for _, f := range rvq.ctx.Fields {
		if !roomvod.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if rvq.path != nil {
		prev, err := rvq.path(ctx)
		if err != nil {
			return err
		}
		rvq.sql = prev
	}
	return nil
}

func (rvq *RoomVodQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*RoomVod, error) {
	var (
		nodes = []*RoomVod{}
		_spec = rvq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*RoomVod).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &RoomVod{config: rvq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, rvq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (rvq *RoomVodQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := rvq.querySpec()
	_spec.Node.Columns = rvq.ctx.Fields
	if len(rvq.ctx.Fields) > 0 {
		_spec.Unique = rvq.ctx.Unique != nil && *rvq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, rvq.driver, _spec)
}

func (rvq *RoomVodQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(roomvod.Table, roomvod.Columns, sqlgraph.NewFieldSpec(roomvod.FieldID, field.TypeUint64))
	_spec.From = rvq.sql
	if unique := rvq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if rvq.path != nil {
		_spec.Unique = true
	}
	if fields := rvq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, roomvod.FieldID)
		for i := range fields {
			if fields[i] != roomvod.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := rvq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := rvq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := rvq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := rvq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (rvq *RoomVodQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(rvq.driver.Dialect())
	t1 := builder.Table(roomvod.Table)
	columns := rvq.ctx.Fields
	if len(columns) == 0 {
		columns = roomvod.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if rvq.sql != nil {
		selector = rvq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if rvq.ctx.Unique != nil && *rvq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range rvq.predicates {
		p(selector)
	}
	for _, p := range rvq.order {
		p(selector)
	}
	if offset := rvq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := rvq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// RoomVodGroupBy is the group-by builder for RoomVod entities.
type RoomVodGroupBy struct {
	selector
	build *RoomVodQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (rvgb *RoomVodGroupBy) Aggregate(fns ...AggregateFunc) *RoomVodGroupBy {
	rvgb.fns = append(rvgb.fns, fns...)
	return rvgb
}

// Scan applies the selector query and scans the result into the given value.
func (rvgb *RoomVodGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, rvgb.build.ctx, "GroupBy")
	if err := rvgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*RoomVodQuery, *RoomVodGroupBy](ctx, rvgb.build, rvgb, rvgb.build.inters, v)
}

func (rvgb *RoomVodGroupBy) sqlScan(ctx context.Context, root *RoomVodQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(rvgb.fns))
	for _, fn := range rvgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*rvgb.flds)+len(rvgb.fns))
		for _, f := range *rvgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*rvgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := rvgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// RoomVodSelect is the builder for selecting fields of RoomVod entities.
type RoomVodSelect struct {
	*RoomVodQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (rvs *RoomVodSelect) Aggregate(fns ...AggregateFunc) *RoomVodSelect {
	rvs.fns = append(rvs.fns, fns...)
	return rvs
}

// Scan applies the selector query and scans the result into the given value.
func (rvs *RoomVodSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, rvs.ctx, "Select")
	if err := rvs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*RoomVodQuery, *RoomVodSelect](ctx, rvs.RoomVodQuery, rvs, rvs.inters, v)
}

func (rvs *RoomVodSelect) sqlScan(ctx context.Context, root *RoomVodQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(rvs.fns))
	for _, fn := range rvs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*rvs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := rvs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
