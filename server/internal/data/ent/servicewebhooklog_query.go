// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"faceto-ai/internal/data/ent/predicate"
	"faceto-ai/internal/data/ent/servicewebhooklog"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// ServiceWebhookLogQuery is the builder for querying ServiceWebhookLog entities.
type ServiceWebhookLogQuery struct {
	config
	ctx        *QueryContext
	order      []servicewebhooklog.OrderOption
	inters     []Interceptor
	predicates []predicate.ServiceWebhookLog
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ServiceWebhookLogQuery builder.
func (swlq *ServiceWebhookLogQuery) Where(ps ...predicate.ServiceWebhookLog) *ServiceWebhookLogQuery {
	swlq.predicates = append(swlq.predicates, ps...)
	return swlq
}

// Limit the number of records to be returned by this query.
func (swlq *ServiceWebhookLogQuery) Limit(limit int) *ServiceWebhookLogQuery {
	swlq.ctx.Limit = &limit
	return swlq
}

// Offset to start from.
func (swlq *ServiceWebhookLogQuery) Offset(offset int) *ServiceWebhookLogQuery {
	swlq.ctx.Offset = &offset
	return swlq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (swlq *ServiceWebhookLogQuery) Unique(unique bool) *ServiceWebhookLogQuery {
	swlq.ctx.Unique = &unique
	return swlq
}

// Order specifies how the records should be ordered.
func (swlq *ServiceWebhookLogQuery) Order(o ...servicewebhooklog.OrderOption) *ServiceWebhookLogQuery {
	swlq.order = append(swlq.order, o...)
	return swlq
}

// First returns the first ServiceWebhookLog entity from the query.
// Returns a *NotFoundError when no ServiceWebhookLog was found.
func (swlq *ServiceWebhookLogQuery) First(ctx context.Context) (*ServiceWebhookLog, error) {
	nodes, err := swlq.Limit(1).All(setContextOp(ctx, swlq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{servicewebhooklog.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (swlq *ServiceWebhookLogQuery) FirstX(ctx context.Context) *ServiceWebhookLog {
	node, err := swlq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ServiceWebhookLog ID from the query.
// Returns a *NotFoundError when no ServiceWebhookLog ID was found.
func (swlq *ServiceWebhookLogQuery) FirstID(ctx context.Context) (id uint64, err error) {
	var ids []uint64
	if ids, err = swlq.Limit(1).IDs(setContextOp(ctx, swlq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{servicewebhooklog.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (swlq *ServiceWebhookLogQuery) FirstIDX(ctx context.Context) uint64 {
	id, err := swlq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ServiceWebhookLog entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ServiceWebhookLog entity is found.
// Returns a *NotFoundError when no ServiceWebhookLog entities are found.
func (swlq *ServiceWebhookLogQuery) Only(ctx context.Context) (*ServiceWebhookLog, error) {
	nodes, err := swlq.Limit(2).All(setContextOp(ctx, swlq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{servicewebhooklog.Label}
	default:
		return nil, &NotSingularError{servicewebhooklog.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (swlq *ServiceWebhookLogQuery) OnlyX(ctx context.Context) *ServiceWebhookLog {
	node, err := swlq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ServiceWebhookLog ID in the query.
// Returns a *NotSingularError when more than one ServiceWebhookLog ID is found.
// Returns a *NotFoundError when no entities are found.
func (swlq *ServiceWebhookLogQuery) OnlyID(ctx context.Context) (id uint64, err error) {
	var ids []uint64
	if ids, err = swlq.Limit(2).IDs(setContextOp(ctx, swlq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{servicewebhooklog.Label}
	default:
		err = &NotSingularError{servicewebhooklog.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (swlq *ServiceWebhookLogQuery) OnlyIDX(ctx context.Context) uint64 {
	id, err := swlq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ServiceWebhookLogs.
func (swlq *ServiceWebhookLogQuery) All(ctx context.Context) ([]*ServiceWebhookLog, error) {
	ctx = setContextOp(ctx, swlq.ctx, "All")
	if err := swlq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ServiceWebhookLog, *ServiceWebhookLogQuery]()
	return withInterceptors[[]*ServiceWebhookLog](ctx, swlq, qr, swlq.inters)
}

// AllX is like All, but panics if an error occurs.
func (swlq *ServiceWebhookLogQuery) AllX(ctx context.Context) []*ServiceWebhookLog {
	nodes, err := swlq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ServiceWebhookLog IDs.
func (swlq *ServiceWebhookLogQuery) IDs(ctx context.Context) (ids []uint64, err error) {
	if swlq.ctx.Unique == nil && swlq.path != nil {
		swlq.Unique(true)
	}
	ctx = setContextOp(ctx, swlq.ctx, "IDs")
	if err = swlq.Select(servicewebhooklog.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (swlq *ServiceWebhookLogQuery) IDsX(ctx context.Context) []uint64 {
	ids, err := swlq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (swlq *ServiceWebhookLogQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, swlq.ctx, "Count")
	if err := swlq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, swlq, querierCount[*ServiceWebhookLogQuery](), swlq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (swlq *ServiceWebhookLogQuery) CountX(ctx context.Context) int {
	count, err := swlq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (swlq *ServiceWebhookLogQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, swlq.ctx, "Exist")
	switch _, err := swlq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (swlq *ServiceWebhookLogQuery) ExistX(ctx context.Context) bool {
	exist, err := swlq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ServiceWebhookLogQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (swlq *ServiceWebhookLogQuery) Clone() *ServiceWebhookLogQuery {
	if swlq == nil {
		return nil
	}
	return &ServiceWebhookLogQuery{
		config:     swlq.config,
		ctx:        swlq.ctx.Clone(),
		order:      append([]servicewebhooklog.OrderOption{}, swlq.order...),
		inters:     append([]Interceptor{}, swlq.inters...),
		predicates: append([]predicate.ServiceWebhookLog{}, swlq.predicates...),
		// clone intermediate query.
		sql:  swlq.sql.Clone(),
		path: swlq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		UUID string `json:"uuid,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ServiceWebhookLog.Query().
//		GroupBy(servicewebhooklog.FieldUUID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (swlq *ServiceWebhookLogQuery) GroupBy(field string, fields ...string) *ServiceWebhookLogGroupBy {
	swlq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ServiceWebhookLogGroupBy{build: swlq}
	grbuild.flds = &swlq.ctx.Fields
	grbuild.label = servicewebhooklog.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		UUID string `json:"uuid,omitempty"`
//	}
//
//	client.ServiceWebhookLog.Query().
//		Select(servicewebhooklog.FieldUUID).
//		Scan(ctx, &v)
func (swlq *ServiceWebhookLogQuery) Select(fields ...string) *ServiceWebhookLogSelect {
	swlq.ctx.Fields = append(swlq.ctx.Fields, fields...)
	sbuild := &ServiceWebhookLogSelect{ServiceWebhookLogQuery: swlq}
	sbuild.label = servicewebhooklog.Label
	sbuild.flds, sbuild.scan = &swlq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ServiceWebhookLogSelect configured with the given aggregations.
func (swlq *ServiceWebhookLogQuery) Aggregate(fns ...AggregateFunc) *ServiceWebhookLogSelect {
	return swlq.Select().Aggregate(fns...)
}

func (swlq *ServiceWebhookLogQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range swlq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, swlq); err != nil {
				return err
			}
		}
	}
	for _, f := range swlq.ctx.Fields {
		if !servicewebhooklog.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if swlq.path != nil {
		prev, err := swlq.path(ctx)
		if err != nil {
			return err
		}
		swlq.sql = prev
	}
	return nil
}

func (swlq *ServiceWebhookLogQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ServiceWebhookLog, error) {
	var (
		nodes = []*ServiceWebhookLog{}
		_spec = swlq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ServiceWebhookLog).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ServiceWebhookLog{config: swlq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, swlq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (swlq *ServiceWebhookLogQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := swlq.querySpec()
	_spec.Node.Columns = swlq.ctx.Fields
	if len(swlq.ctx.Fields) > 0 {
		_spec.Unique = swlq.ctx.Unique != nil && *swlq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, swlq.driver, _spec)
}

func (swlq *ServiceWebhookLogQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(servicewebhooklog.Table, servicewebhooklog.Columns, sqlgraph.NewFieldSpec(servicewebhooklog.FieldID, field.TypeUint64))
	_spec.From = swlq.sql
	if unique := swlq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if swlq.path != nil {
		_spec.Unique = true
	}
	if fields := swlq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, servicewebhooklog.FieldID)
		for i := range fields {
			if fields[i] != servicewebhooklog.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := swlq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := swlq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := swlq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := swlq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (swlq *ServiceWebhookLogQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(swlq.driver.Dialect())
	t1 := builder.Table(servicewebhooklog.Table)
	columns := swlq.ctx.Fields
	if len(columns) == 0 {
		columns = servicewebhooklog.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if swlq.sql != nil {
		selector = swlq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if swlq.ctx.Unique != nil && *swlq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range swlq.predicates {
		p(selector)
	}
	for _, p := range swlq.order {
		p(selector)
	}
	if offset := swlq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := swlq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ServiceWebhookLogGroupBy is the group-by builder for ServiceWebhookLog entities.
type ServiceWebhookLogGroupBy struct {
	selector
	build *ServiceWebhookLogQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (swlgb *ServiceWebhookLogGroupBy) Aggregate(fns ...AggregateFunc) *ServiceWebhookLogGroupBy {
	swlgb.fns = append(swlgb.fns, fns...)
	return swlgb
}

// Scan applies the selector query and scans the result into the given value.
func (swlgb *ServiceWebhookLogGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, swlgb.build.ctx, "GroupBy")
	if err := swlgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ServiceWebhookLogQuery, *ServiceWebhookLogGroupBy](ctx, swlgb.build, swlgb, swlgb.build.inters, v)
}

func (swlgb *ServiceWebhookLogGroupBy) sqlScan(ctx context.Context, root *ServiceWebhookLogQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(swlgb.fns))
	for _, fn := range swlgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*swlgb.flds)+len(swlgb.fns))
		for _, f := range *swlgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*swlgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := swlgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ServiceWebhookLogSelect is the builder for selecting fields of ServiceWebhookLog entities.
type ServiceWebhookLogSelect struct {
	*ServiceWebhookLogQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (swls *ServiceWebhookLogSelect) Aggregate(fns ...AggregateFunc) *ServiceWebhookLogSelect {
	swls.fns = append(swls.fns, fns...)
	return swls
}

// Scan applies the selector query and scans the result into the given value.
func (swls *ServiceWebhookLogSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, swls.ctx, "Select")
	if err := swls.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ServiceWebhookLogQuery, *ServiceWebhookLogSelect](ctx, swls.ServiceWebhookLogQuery, swls, swls.inters, v)
}

func (swls *ServiceWebhookLogSelect) sqlScan(ctx context.Context, root *ServiceWebhookLogQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(swls.fns))
	for _, fn := range swls.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*swls.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := swls.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
